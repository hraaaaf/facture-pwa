// Exact user-phrase runtime certification for Factea voice input.
import assert from 'node:assert/strict'
import { spawn } from 'node:child_process'
import fs from 'node:fs/promises'
import process from 'node:process'
import { chromium } from 'playwright'

const port = 4178
const origin = `http://127.0.0.1:${port}`
const outputDir = 'artifacts/voice-input-v1'
const widths = [390, 430, 768]
const viewportHeight = width => width <= 430 ? 844 : 1024
const transcript = "le client c'est Pierre, l'objet c'est des draps, la désignation c'est des draps de 250 centimètres, le prix unitaire c'est 150 dirhams, la quantité c'est six articles"

const server = spawn('npm', ['run', 'dev', '--', '--host', '127.0.0.1', '--port', String(port)], { stdio: ['ignore', 'pipe', 'pipe'], env: { ...process.env }, detached: process.platform !== 'win32' })
let serverLog = ''
server.stdout.on('data', chunk => { serverLog += chunk.toString() })
server.stderr.on('data', chunk => { serverLog += chunk.toString() })
const stopServer = () => { if (!server.pid) return; try { if (process.platform === 'win32') server.kill('SIGTERM'); else process.kill(-server.pid, 'SIGTERM') } catch (error) { if (error?.code !== 'ESRCH') throw error } }
const waitForServer = async () => { const deadline = Date.now() + 30_000; while (Date.now() < deadline) { try { const response = await fetch(`${origin}/`); if (response.ok) return } catch {} await new Promise(resolve => setTimeout(resolve, 250)) } throw new Error(`Vite indisponible.\n${serverLog}`) }
const installFakeSpeechRecognition = async page => { await page.addInitScript(({ sample }) => { class FakeSpeechRecognition { constructor() { this.lang='fr-FR'; this.interimResults=true; this.continuous=true; this.onresult=null; this.onerror=null; this.onend=null } start() { queueMicrotask(() => this.onresult?.({ resultIndex:0, results:[{ isFinal:true, 0:{ transcript:sample } }] })) } stop(){queueMicrotask(()=>this.onend?.())} abort(){queueMicrotask(()=>this.onend?.())} } window.SpeechRecognition=FakeSpeechRecognition; window.webkitSpeechRecognition=FakeSpeechRecognition }, { sample: transcript }) }
const seedConfiguredCompany = async page => { await page.goto(`${origin}/`, { waitUntil:'domcontentloaded' }); await page.evaluate(async () => { const company={name:'Benmoussa Rachid',brand:'TAPISTOR SABRE',address:'484, Cit Amal 5, MASSIRA, CYM, RABAT',cityLabel:'RABAT',phone:'',fax:'',email:'',ice:'001806241000086',ifNumber:'35789182',rc:'82972 RABAT',patente:'26450045',cnss:'7121982',bankName:'',rib:'181 810 21211 52654410108 03',legalLine:'',defaultVatRate:20,logoDataUrl:'',managerSignatureDataUrl:'',pdfTemplate:'premium',onboardingCompleted:true,numberingPrefixes:{DEVIS:'DEV',FACTURE:'F',BL:'BL',BC:'BC'}}; await new Promise((resolve,reject)=>{const request=indexedDB.open('facture-pwa',3);request.onupgradeneeded=()=>{const db=request.result;if(!db.objectStoreNames.contains('documents'))db.createObjectStore('documents',{keyPath:'id'});if(!db.objectStoreNames.contains('settings'))db.createObjectStore('settings');if(!db.objectStoreNames.contains('counters'))db.createObjectStore('counters',{keyPath:'key'});if(!db.objectStoreNames.contains('clients'))db.createObjectStore('clients',{keyPath:'id'});if(!db.objectStoreNames.contains('catalog'))db.createObjectStore('catalog',{keyPath:'id'})};request.onerror=()=>reject(request.error);request.onsuccess=()=>{const db=request.result;const tx=db.transaction('settings','readwrite');tx.objectStore('settings').put(company,'company');tx.onerror=()=>{db.close();reject(tx.error)};tx.onabort=()=>{db.close();reject(tx.error)};tx.oncomplete=()=>{db.close();resolve()}}}) }); await page.reload({waitUntil:'networkidle'}) }

const report={viewports:{},e2e:{},consoleErrors:[]}; let browser
try {
  await fs.mkdir(outputDir,{recursive:true}); await waitForServer(); browser=await chromium.launch({headless:true})
  for (const width of widths) {
    const context=await browser.newContext({viewport:{width,height:viewportHeight(width)}}); const page=await context.newPage(); await installFakeSpeechRecognition(page); const errors=[]; page.on('console',m=>{if(m.type()==='error')errors.push(m.text())}); page.on('pageerror',e=>errors.push(e.message)); await seedConfiguredCompany(page)
    await page.locator('.fab').waitFor({timeout:15000}); await page.locator('.fab').click(); await page.locator('.new-sheet').waitFor(); await page.locator('.import-quote-card').click(); await page.locator('.quote-import-sheet').waitFor()
    const pickerMetrics=await page.evaluate(()=>{const sheet=document.querySelector('.quote-import-sheet')?.getBoundingClientRect(),close=document.querySelector('.quote-import-header .sheet-close')?.getBoundingClientRect(),voice=document.querySelector('.quote-voice-choice')?.getBoundingClientRect(),style=document.querySelector('.quote-voice-choice')?getComputedStyle(document.querySelector('.quote-voice-choice')):null;return{viewport:innerWidth,scrollWidth:document.documentElement.scrollWidth,sheetWidth:sheet?.width??0,closeWidth:close?.width??0,closeHeight:close?.height??0,voiceHeight:voice?.height??0,voiceRadius:style?.borderRadius??'',sourceButtons:document.querySelectorAll('.quote-format-grid > button').length}})
    assert.ok(pickerMetrics.scrollWidth<=pickerMetrics.viewport); assert.ok(pickerMetrics.sheetWidth<=pickerMetrics.viewport); assert.ok(pickerMetrics.closeWidth>=44&&pickerMetrics.closeHeight>=44); assert.ok(pickerMetrics.voiceHeight>=90); assert.equal(pickerMetrics.sourceButtons,5); report.viewports[width]={picker:pickerMetrics}; await page.screenshot({path:`${outputDir}/after-picker-${width}.png`,fullPage:false})
    await page.locator('.quote-voice-choice').click(); await page.locator('.quote-voice-panel').waitFor(); const textarea=page.getByRole('textbox',{name:'Transcription du message vocal'}); await page.getByRole('button',{name:'Commencer'}).click(); await page.getByRole('button',{name:'Arrêter'}).waitFor(); await page.waitForFunction(()=>document.querySelector('textarea[aria-label="Transcription du message vocal"]')?.value.length>10)
    const voiceMetrics=await page.evaluate(()=>{const panel=document.querySelector('.quote-voice-panel')?.getBoundingClientRect(),textarea=document.querySelector('.quote-voice-panel textarea')?.getBoundingClientRect(),actions=[...document.querySelectorAll('.quote-voice-actions button')].map(b=>b.getBoundingClientRect().height),orb=document.querySelector('.quote-voice-orb')?.getBoundingClientRect();return{viewport:innerWidth,scrollWidth:document.documentElement.scrollWidth,panelWidth:panel?.width??0,textareaWidth:textarea?.width??0,minActionHeight:actions.length?Math.min(...actions):0,orbWidth:orb?.width??0,listening:document.querySelector('.quote-voice-orb')?.classList.contains('listening')??false}})
    assert.ok(voiceMetrics.scrollWidth<=voiceMetrics.viewport); assert.ok(voiceMetrics.panelWidth<=pickerMetrics.sheetWidth); assert.ok(voiceMetrics.textareaWidth<=voiceMetrics.panelWidth); assert.ok(voiceMetrics.minActionHeight>=48); assert.ok(voiceMetrics.orbWidth>=70); assert.equal(voiceMetrics.listening,true); report.viewports[width].voice=voiceMetrics; await page.screenshot({path:`${outputDir}/after-listening-${width}.png`,fullPage:false}); await page.getByRole('button',{name:'Arrêter'}).click()
    if(width===390){assert.match(await textarea.inputValue(),/Pierre/);assert.match(await textarea.inputValue(),/six articles/i);await page.getByRole('button',{name:'Analyser'}).click();await page.locator('.quote-ready-hero').waitFor({timeout:15000});assert.match(await page.locator('.quote-ready-hero').innerText(),/Pierre/);const readyGrid=await page.locator('.quote-ready-grid').innerText();assert.match(readyGrid,/1/);assert.match(readyGrid.replace(/[\s\u202f\u00a0]/g,''),/900MAD/);await page.screenshot({path:`${outputDir}/after-ready-390.png`,fullPage:false});await page.getByRole('button',{name:'Créer le devis'}).click();await page.locator('.editor-screen').waitFor();assert.equal(await page.locator('.client-field input').inputValue(),'Pierre');assert.equal(await page.locator('.article-card').count(),1);assert.match(await page.locator('.article-designation textarea').first().inputValue(),/draps de 250 centimètres/i);const numericValues=await page.locator('.compact-number-field input').evaluateAll(inputs=>inputs.map(input=>input.value));assert.ok(numericValues.includes('6'));assert.ok(numericValues.includes('150'));await page.screenshot({path:`${outputDir}/after-editor-390.png`,fullPage:false});report.e2e={transcript,reviewFields:0,result:'editable DEVIS draft',lineCount:1,client:'Pierre',quantity:6,unitPriceHT:150,totalHT:900}}
    report.consoleErrors.push(...errors.map(error=>`${width}px: ${error}`)); await context.close()
  }
  assert.deepEqual(report.consoleErrors,[]); await fs.writeFile(`${outputDir}/report.json`,JSON.stringify({ok:true,...report},null,2)); console.log(JSON.stringify({ok:true,...report},null,2))
} finally { if(browser)await browser.close(); stopServer() }
